# Changelog

## 2026-03-30
- 将 `rules.md` 拆分为 `rules/*.md`，结构与仓库根目录 `.ai` 保持一致。
- 为 `packages/web` 新增 `INDEX.md`、`CHANGELOG.md`。
- 统一规则文档格式为 `Priority`、`Scope`、`Rule`、`Non-Goals`、`Exceptions`、`Checks`。
- 新增前后端分页一致性规则：`rules/81-api-pagination-consistency.md`（统一 `page/pageSize` 协议）。
